package com.revature.features;

import org.junit.runner.RunWith;

import io.cucumber.junit.platform.engine.Cucumber;

@Cucumber
public class TestRunner {
	// The TestRunner will take the scenarios from the feature file (add.feature)
	// And treat each scenario as a single test
	
	// Since we have 2 scenarios at the moment, we have 2 tests
}
